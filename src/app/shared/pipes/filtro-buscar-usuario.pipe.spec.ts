import { FiltroBuscarUsuarioPipe } from './filtro-buscar-usuario.pipe';

describe('FiltroBuscarUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBuscarUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
