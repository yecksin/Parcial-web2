import { FiltroBuscarMensajesPipe } from './filtro-buscar-mensajes.pipe';

describe('FiltroBuscarMensajesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBuscarMensajesPipe();
    expect(pipe).toBeTruthy();
  });
});
