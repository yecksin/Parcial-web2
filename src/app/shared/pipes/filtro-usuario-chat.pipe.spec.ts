import { FiltroUsuarioChatPipe } from './filtro-usuario-chat.pipe';

describe('FiltroUsuarioChatPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUsuarioChatPipe();
    expect(pipe).toBeTruthy();
  });
});
